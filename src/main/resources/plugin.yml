main: # inject
name: # inject
version: ${version}
authors:
  - dongzh1
api-version: "1.21"
folia-supported: true
update-info: # inject
depend-list: # inject
relocate: # inject
kotlin-version: # inject
softdepend:
  - CraftEngine
  - ChunkWorld
  - EasyLevels
  - MythicMobs
  - PlaceholderAPI
  - BetterHud
commands:
  sourceforge:
    description: SourceForge main command
    usage: /sourceforge <forge|reload|validate|giveblueprint|giveequipment|givematerial|give|testdamage|reroll|upgrade|stats|debug>
    aliases:
      - sf
permissions:
  sourceforge.admin:
    description: Allows SourceForge admin commands
    default: op
