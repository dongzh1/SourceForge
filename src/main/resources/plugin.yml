main: # inject
name: # inject
version: ${version}
authors:
  - dongzh1
api-version: "1.21"
folia-supported: true
update-info: # inject
depend-list: # inject
relocate: # inject
kotlin-version: # inject
softdepend:
  - CraftEngine
  - ChunkWorld
  - EasyLevels
  - PlaceholderAPI
commands:
  sourceforge:
    description: SourceForge main command
    usage: /sourceforge <forge|reload|validate|giveblueprint|giveequipment|givematerial|give|testdamage|mmdamage|reroll|upgrade|enchant|debug>
    aliases:
      - sf
permissions:
  sourceforge.admin:
    description: Allows SourceForge admin commands
    default: op
